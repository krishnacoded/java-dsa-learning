/*Write a program that calculates the **area of a circle** using the formula:
Area = π × r × r

Use `Math.PI`. */
public class problem19_operators {
    public static void main(String[] args) {
        int radius = 21;
        double area = Math.PI * radius * radius;
        System.out.println("The area of the circle is " + area);
    }
}